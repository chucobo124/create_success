<form class="form-wrapper">
  <input type="text" id="search" placeholder="Search for..." required>
  <input type="submit" value="go" id="submit">
</form>
<%= link_to t('news.create'), new_admin_news_path%>

<table>
  <tr>
    <th><%= t('news.title')%></th>
    <th><%= t('news.description')%></th>
    <th><%= t('aciton.label')%></th>
  </tr>
  <% @admin_news.each do |news|%>
    <tr>
      <th><%= news.title%></th>
      <th><%= news.description.html_safe%></th>
      <th>
        <%= link_to t('action.update'), admin_news_path(news.id)%>
        <%= link_to t('action.delete'), admin_news_path(news.id), method: 'delete'%>
      </th>
    </tr>
  <%end%>
</table>
